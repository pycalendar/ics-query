version: 2
formats: all
build:
  os: ubuntu-24.04
  tools:
    python: "3.14"
  commands:
    - git fetch --tags
    - pip install -r docs/requirements.txt
    - pip install -e .
    - sphinx-build docs/ $READTHEDOCS_OUTPUT/html/
